/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.env
.env*.local
.DS_Store
/database/raw_scrape.json
/database/_img_urls.txt
npm-debug.log*

# 병원 내부자료 / 스크래핑 원본 (공개 repo 유출 금지)
/onboarding/
/database/clinic.raw.json
/database/pricing-raw.md
/scrape_tmp/

# verification artifacts
/.playwright-mcp/
/*.jpeg
/en-*.jpeg
/ja-*.jpeg
/prod-*.jpeg

# tooling config / audit logs (NOT part of website; may contain tokens)
/.bkit/
/.claude/

# 추가 온보딩 자료 (병원 내부 — 유출 금지)
/onboarding2/

# B&A·시술별 자료(내부)
/onboarding3/

# 온보딩 원본 자료 폴더 (B&A 환자사진·로고 .ai 등 내부 — 유출 금지)
/원데이 온보딩 자료/

# 병원 원내자료 (내부 — 유출 금지)
/원데이 원내자료/
*.crdownload
*.tsbuildinfo.claude

# 대용량 zip (원내자료 압축본 등)
*.zip

# 티아나 잔여(원데이 무관)
/tiana*.png

# 캡션/본문 작업파일 (내부 — 사이트 자산 아님)
/*.txt
